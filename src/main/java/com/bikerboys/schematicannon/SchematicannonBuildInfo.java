package com.bikerboys.schematicannon;

/** Build metadata kept intentionally independent from Create's build pipeline. */
public final class SchematicannonBuildInfo {
    public static final String VERSION = "1.0.12-fabric.26.2";
    public static final String GIT_COMMIT = "development";

    private SchematicannonBuildInfo() {
    }
}
